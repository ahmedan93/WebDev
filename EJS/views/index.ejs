<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h1> hiiiiiiii <%= title %> </h1>

    <p> Current seconds <%= seconds %></p>

    <% if (seconds % 2 === 0) {%>
    <ul>
        <% items.forEach((element) => {%>
        <li><%= element %></li>
        <%}) %>
    </ul>
    <%} else {%>
<p>
    No items to display
</p>
        <% } %>

        <%- htmlContent %>
    
</body>

<%- include("footer.ejs") %>

</html>